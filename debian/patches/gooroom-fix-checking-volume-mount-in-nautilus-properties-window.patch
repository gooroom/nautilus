diff --git a/src/nautilus-properties-window.c b/src/nautilus-properties-window.c
index cf0d4d4..a46a7fd 100644
--- a/src/nautilus-properties-window.c
+++ b/src/nautilus-properties-window.c
@@ -2281,6 +2281,30 @@ is_burn_directory (NautilusFile *file)
     return strcmp (file_uri, "burn:///") == 0;
 }
 
+static gboolean
+get_is_mounted (NautilusFile *file)
+{
+    GFile *location;
+    GFileInfo *info;
+    gboolean is_mounted = FALSE;
+
+    location = nautilus_file_get_location (file);
+    info = g_file_query_info (location,
+                              G_FILE_ATTRIBUTE_UNIX_IS_MOUNTPOINT,
+                              G_FILE_QUERY_INFO_NOFOLLOW_SYMLINKS,
+                              NULL, NULL);
+    if (info)
+    {
+        if (g_file_info_has_attribute (info, G_FILE_ATTRIBUTE_UNIX_IS_MOUNTPOINT))
+        {
+            is_mounted = g_file_info_get_attribute_boolean (info, G_FILE_ATTRIBUTE_UNIX_IS_MOUNTPOINT);
+        }
+        g_object_unref (info);
+    }
+    g_object_unref (location);
+
+    return is_mounted;
+}
 
 static gboolean
 is_volume_properties (NautilusPropertiesWindow *self)
@@ -2305,6 +2329,11 @@ is_volume_properties (NautilusPropertiesWindow *self)
         return FALSE;
     }
 
+    if (get_is_mounted (file))
+    {
+        return TRUE;
+    }
+
     if (nautilus_file_can_unmount (file))
     {
         return TRUE;
